# MCP 接入

照献工程启动后会在本机 `127.0.0.1` 创建带随机令牌的桥接服务。令牌只写入当前用户的应用数据目录，不应提交到仓库或发送给其他人。

安装依赖后运行：

```powershell
npm run codex:mcp
```

该命令会把 `zx-engineering` MCP 服务写入当前用户的 Codex 配置，同时保留其他已有配置。然后启动照献工程，并在 Codex 中重新开始一个任务。

也可以单独启动服务进行调试：

```powershell
npm run mcp
```

执行写入前请确认已打开正确世界，并使用事务和读取校验。创建建筑计划只保存计划信息，不会自动生成地图方块。
